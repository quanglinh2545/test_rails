<%= form_with(model: manga_review, local: true) do |form| %>
  <% if manga_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manga_review.errors.count, "error") %> prohibited this manga_review from being saved:</h2>

      <ul>
        <% manga_review.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :manga_id %>
    <%= form.number_field :manga_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
